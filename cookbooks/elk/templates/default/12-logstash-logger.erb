input {
  tcp {
    host => "0.0.0.0"
    port => 5228
    codec => json_lines
    ssl_cert => "/etc/pki/tls/certs/logstash-forwarder.crt"
    ssl_key => "/etc/pki/tls/private/logstash-forwarder.key"
  }
}

